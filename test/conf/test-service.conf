[SERVICE]
    flush        1
    grace        2
    log_level    debug
    parsers_file /fluent-bit/etc/couchbase/parsers-couchbase.conf
    HTTP_Server     On
    storage.metrics On
    storage.path /tmp/buffers