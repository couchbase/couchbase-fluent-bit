[SERVICE]
    flush           1
    log_level       Warning
    parsers_file    /fluent-bit/etc/parsers-couchbase.conf
    HTTP_Server     On
    storage.metrics On
    HTTP_Port       ${HTTP_PORT}
    Health_Check    On
    storage.path    ${STORAGE_BUFFER_PATH}