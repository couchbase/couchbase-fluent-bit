---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  namespace: monitoring
spec:
  defaultBackend:
    service:
      name: loki-grafana
      port:
        number: 3000
---
